# Set this on Vercel for Production before rebuilding the frontend.
# Replace with the ACTUAL Render service URL. Do not include /api.
DJANGO_API_ORIGIN=https://YOUR-ACTUAL-SERVICE.onrender.com
# Never add DATABASE_URL or DJANGO_SECRET_KEY to the frontend.
